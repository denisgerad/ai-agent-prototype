from langchain.memory import ConversationBufferMemory

memory = ConversationBufferMemory(
    memory_key="chat_history",
    return_messages=True
)

agent = initialize_agent(
    tools=tools,
    llm=llm,
    agent=AgentType.ZERO_SHOT_REACT_DESCRIPTION,
    verbose=True,
    memory=memory
)

//example conversation
User: Summarize the PDF
Agent: (summary)

User: Focus only on security aspects
Agent: (uses previous context + PDF)

User: Compare that with industry best practices
Agent: (builds on topic)

New Chat = New Topic
memory.clear()

Conversation Summary Memory
from langchain.memory import ConversationSummaryMemory

memory = ConversationSummaryMemory(
    llm=llm,
    memory_key="chat_history",
    return_messages=True
)

streamlit run app_streamlit.py

Done! Changed verbose=True to verbose=False in both files. Now when you ask 
"what is one tap trading", you'll get only the clean answer without the 
Thought/Action/Observation steps.